package exam.spaceshooter;

public class AsteroidFactory {
    /*public Asteroid createAsteroid(String type, int x, int y, int velocity) {
        Asteroid asteroid = null;

        if (type.equals("Small")) {
            asteroid = new SmallAsteroid();
        } else if (type.equals("Medium")) {
            asteroid = new MediumAsteroid();
        } else if (type.equals("Large")) {
            asteroid = new LargeAsteroid();
        }

        // Code to set the asteroid's starting position and velocity

        return asteroid;
    }*/
}
